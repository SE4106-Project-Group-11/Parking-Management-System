/*const express = require('express');
const router = express.Router();
const { createViolation, getViolationsForUser } = require('../controllers/violationController');

router.post('/', createViolation);
router.get('/user', getViolationsForUser);

module.exports = router;
*/